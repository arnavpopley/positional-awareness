# Agent instructions

This repo is **Positional Awareness**. Always read `SPEC.md` first.

Implement only the phase the user named. Default is **Phase 0** (see `BUILD_PROMPT.md`). Do not scaffold Telegram, a web app, Nifty alpha, or order placement.

Python 3.12 + uv. SQLite. CLI + macOS notifications in v1.
