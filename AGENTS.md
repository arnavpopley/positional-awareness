# Agent instructions

This repo is **Positional Awareness**. Always read `SPEC.md` first.

Implement only the phase the user named. Default is **Phase 0** (see `BUILD_PROMPT.md`). Do not scaffold Telegram, a public web app, Nifty alpha, or order placement. A read-only localhost page is allowed (`pos web`).

Python 3.12 + uv. SQLite. CLI + macOS notifications + local page.
