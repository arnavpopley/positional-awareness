# Agent instructions

This repo is **Positional Awareness**. Always read `SPEC.md` first.

Implement only the phase the user named. Default is **Phase 0** (see `BUILD_PROMPT.md`). Do not scaffold Telegram, Nifty alpha, or order placement. Localhost `pos web` is allowed. `pos deploy` is a password-gated Vercel snapshot of that page; never commit `site/` or `PA_SITE_PASSWORD`.

Python 3.12 + uv. SQLite. CLI + macOS notifications + local page.
